import React from 'react';

export const Navbar = () => {
    return (
        <nav> 
            <header>Media Posts</header>
            <div className="user-profile">
            icon
            </div>
        </nav>
    );
};

export default Navbar;